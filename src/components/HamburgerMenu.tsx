import * as React from 'react'
import {NavLinks} from "../myInterfaces/SiteData";

interface ThisProps {
  navLinks: NavLinks
}

export default ({ navLinks }: ThisProps) => {
  return ( // TODO(Hamburger menu for mobile version)
    <div>

    </div>
  )
}